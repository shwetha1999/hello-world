#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    int i,flag=0;
   char s1[10];
   char s2[10];

   printf("passward: ");
   gets(s2);
   strcpy(s1,s2);
   printf("%s",s1);
   return 0;
}

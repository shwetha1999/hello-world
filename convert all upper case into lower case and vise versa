#include <stdio.h>
#include <string.h>
int main ()
{
char s[40];
int i;
 printf("enter a string\n");
 gets(s);
 puts(s);

 for(i=0;s[i]!='\0';i++)
 {
    if(s[i]>=65 && s[i]<=90)
      s[i]=s[i]+32;

    else
     if(s[i]>=97 && s[i]<=122)
      s[i]=s[i]-32;
 }
puts(s);

return 0;
}
